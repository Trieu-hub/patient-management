21/5
hiểu cách cấu hình localstack và đã chạy thêm 1 vài lệnh cơ bản aws trong git bash như: aws --version, aws configure và quan trọng nhất là aws --endpoint-url: http://localhost:4566 lambda list-functions (lệnh này đặc biệt quan trọng vì nó sẽ cho h thống biết nên trả data về đâu, ở đây thì mình sẽ cấu hình cho 4566)


